// ? the style of the loader is available in index.css

const Loader = () => {
    return (
        <div className = "flex flex-wrap justify-center items-center">
            <span className = "loader"></span>
        </div>
    );
};

export default Loader;